import React, { useState } from 'react';

function OverallEvaluation(params) {
    return (
        <div className="overall-evaluation">
        <h3>Genel Değerlendirme Sonucu:</h3>
        <textarea rows={10} cols={200} />
    </div>
    );
};

export default OverallEvaluation;
